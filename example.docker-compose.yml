services:
  moni:
    image: podkosa/moni
    working_dir: /moni
    command: uvicorn bot:app --host 0.0.0.0 --port 80
    ports: 
      - "6767:80"
    volumes:
    - './settings.yml:/moni/settings.yml'
    # - './logs/:/moni/logs'                            # If you plan logging to file

  # moni_watchdog:
  #   image: podkosa/moni
  #   working_dir: /moni
  #   command: ./watchdog.py
  #   volumes:
  #   - './settings.yml:/moni/settings.yml'
  #  # - './logs/:/moni/logs'
